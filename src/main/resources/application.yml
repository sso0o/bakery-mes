#spring.application.name=bakery-mes
server:
  address: 0.0.0.0
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bakery_mes?serverTimezone=Asia/Seoul
    username: root
    password: ssoyeon1@3
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # ?? ?? update, ??? validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true